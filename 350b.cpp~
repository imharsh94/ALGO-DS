#include<bits/stdc++.h>
using namespce std;

int main()
{
    int n,k,i=1,t=0;
    cin>>n>>k;
    int idx[n];
    
    for(int i=0; i<n; i++)
	cin>>idx[i];

    while(k-t>0)
    {
	t = t+i;
	i++;
    }
    return 0;
}
