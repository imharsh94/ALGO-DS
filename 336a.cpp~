#include<bits/stdc++.h>
using namespace std;

int main()
{
    set< int,greater > s;
    s.insert(1);
    s.insert(2);
    s.insert(3);
    set< int >::iterator it;

    for(it=s.begin(); it != s.end(); ++it)
	cout<<*it<<' ';
    
    return 0;
}
